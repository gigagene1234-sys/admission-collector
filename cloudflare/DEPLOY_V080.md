# Admission Collector Cloudflare v0.8.0 deployment marker

This marker intentionally triggers the production Cloudflare deployment workflow after the v0.8.0 Session Agent Cloud source was generated and persisted by GitHub Actions.

Expected production worker generation: 0.4.0.
Expected D1 migration: 0004_crawl_frontier.sql.
Authenticated Jinhak browser session material remains device-local and is not accepted by the worker.
