{
  "name": "admission-collector-offload",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply admission-collector-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply admission-collector-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
